
const Button = ({children}) => {
    return (
      <button style={{
        borderRadius: '8px',
        border: 'none',
        padding: '10px 20px',

      }}>{children}</button>
    )
  }
  
  export default Button